# 📧 EMAIL CONFIGURATION FOR DOC-ANALYZER
# Copy this file to .env and fill in your email settings

# ================================================================
# 🔧 EMAIL PROVIDER SETTINGS
# ================================================================

# Choose your email provider: gmail, outlook, sendgrid, custom
EMAIL_PROVIDER=gmail

# ================================================================
# 📨 SMTP CREDENTIALS
# ================================================================

# Your email address
SMTP_EMAIL=your-email@gmail.com

# Your email password or app password
# For Gmail: Use App Password (https://support.google.com/accounts/answer/185833)
# For Outlook: Use your regular password or app password
SMTP_PASSWORD=your-app-password

# ================================================================
# 🌐 SMTP SERVER SETTINGS (Auto-configured based on provider)
# ================================================================

# Only needed if using custom provider
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USE_TLS=true
# SMTP_USE_SSL=false

# ================================================================
# ⏰ TIMEOUT & RETRY SETTINGS
# ================================================================

# Connection timeout in seconds
EMAIL_CONNECTION_TIMEOUT=15

# Send timeout in seconds
EMAIL_SEND_TIMEOUT=30

# Maximum retry attempts
EMAIL_MAX_RETRIES=3

# Delay between retries in seconds
EMAIL_RETRY_DELAY=2

# ================================================================
# 🔐 OTP SETTINGS
# ================================================================

# OTP expiry time in minutes
OTP_EXPIRY_MINUTES=10

# ================================================================
# 📋 PROVIDER-SPECIFIC NOTES
# ================================================================

# GMAIL:
# 1. Enable 2-Factor Authentication
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password (not your regular password)

# OUTLOOK/HOTMAIL:
# 1. Enable 2-Factor Authentication (recommended)
# 2. Generate App Password or use regular password
# 3. Server: smtp-mail.outlook.com, Port: 587

# SENDGRID:
# 1. Create SendGrid account
# 2. Generate API key
# 3. Use 'apikey' as username and API key as password

# ================================================================
# 🚨 TROUBLESHOOTING
# ================================================================

# Common Issues:
# 1. "Operation timed out" → Check EMAIL_CONNECTION_TIMEOUT and EMAIL_SEND_TIMEOUT
# 2. "Authentication failed" → Verify SMTP_EMAIL and SMTP_PASSWORD
# 3. "Connection refused" → Check firewall and network settings
# 4. Gmail "Less secure apps" → Use App Password instead

# Debug endpoints (development only):
# GET /auth/debug/email-config - Check configuration
# POST /auth/debug/test-email - Send test email
